
<h1>users page</h1>
<!--
<h1>Users</h1>

<%= form_tag root_path, method: :get do %>
  <%= text_field_tag :query,
    params[:query],
    class: "form-control",
    placeholder: "Find a user"
  %>

<% end %>
<table class="table">
  <thead>
    <tr>
      <th>First Name</th>
      <th>Last Name</th>
      <th>Email</th>
      <th>Send a friend request</th>
      <th colspan="4"></th>
    </tr>
  </thead>

  <tbody>

    <%  @users.each do |user| %>
      <% if user != current_user %>
      <tr>
        
        <td><%= user.first_name %></td>
      <td> <%= user.last_name %></td>
      <td> <%= user.email %></td>
        <td><%= link_to 'Add a friend', user_friend_requests_path(user.id), method: :post,  class: "btn btn-primary" %></td>
      </tr>
      <% end %>
    <% end %>
  </tbody>
</table>
<br>
 -->
